import 'package:flutter/material.dart';

class QuestionFolder extends StatelessWidget {
  const QuestionFolder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
